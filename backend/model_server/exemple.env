# .env.example for the Model Server Service

# The full URL to the model trainer's endpoint that serves the latest model.
# In a local environment, this might be http://127.0.0.1:8001/latest-model
# In a Docker Compose environment, this should be the service name, e.g., http://model_trainer:8001/latest-model
MODEL_URL=http://model_trainer:8001/latest-model

# The logging level for the application.
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO